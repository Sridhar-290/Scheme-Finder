- [API type shapes](api-type-shapes.md) — UserProfile/SavedScheme fields differ from what the AI subagent assumes; check generated schemas before writing form code.
- [Route paths vs nav links](route-paths.md) — App routes use /find and /ai (short slugs), not /find-benefits or /ai-assistant; always verify App.tsx before linking.
- [AI service pattern](ai-service.md) — ai.ts detects OPENAI_API_KEY at startup; falls back to curated mock responses when absent; uses gpt-4o-mini with conversation history.
